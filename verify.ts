import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { console.log(await prisma.empresa.findMany({ where: { nombre: { contains: 'Mar' } } })); console.log(await prisma.empresa.findMany({ where: { nombre: { contains: 'Dulce Estilo' } } })); } main().finally(() => prisma.$disconnect());
