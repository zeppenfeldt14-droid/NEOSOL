import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const noSubZona = await prisma.empresa.count({ where: { zona: 'CABA', OR: [{ subZona: null }, { subZona: '' }] } }); const withSubZona = await prisma.empresa.count({ where: { zona: 'CABA', subZona: { not: null, not: '' } } }); console.log('Sin subZona:', noSubZona, 'Con subZona:', withSubZona); } main().finally(() => prisma.$disconnect());
