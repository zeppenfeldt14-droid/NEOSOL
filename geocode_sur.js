const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const emps = await prisma.empresa.findMany({ where: { zona: 'Zona SUR' } }); for (let emp of emps) { if (emp.direccion && !emp.latitud) { const q = encodeURIComponent(emp.direccion + ', Buenos Aires, Argentina'); const res = await fetch('https://nominatim.openstreetmap.org/search?format=json&q=' + q, { headers: { 'User-Agent': 'MyApp/1.0' } }); const data = await res.json(); if (data && data.length > 0) { await prisma.empresa.update({ where: { id: emp.id }, data: { latitud: parseFloat(data[0].lat), longitud: parseFloat(data[0].lon) } }); console.log('Geocoded ' + emp.nombre); } else { console.log('Failed ' + emp.nombre); } await new Promise(r => setTimeout(r, 1000)); } } } main().catch(console.error).finally(() => prisma.$disconnect());
