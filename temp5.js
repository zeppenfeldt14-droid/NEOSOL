const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const pedidos = await prisma.pedido.findMany({ where: { OR: [{ empresa: { zona: 'CABA' } }, { empresa: { cuit: '30612825749' } }] }, include: { empresa: true, detalles: { include: { producto: true } } } }); console.log(JSON.stringify(pedidos.filter(p => p.empresa.nombre.toLowerCase().includes('max') || p.detalles.length === 0 || p.creadoEn > new Date('2026-05-01')), null, 2)); } main().catch(console.error).finally(() => prisma.$disconnect());
