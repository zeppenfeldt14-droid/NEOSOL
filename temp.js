const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const pedidos = await prisma.pedido.findMany({ where: { empresa: { nombre: { contains: 'Max' } } }, include: { empresa: true, detalles: true } }); console.log(JSON.stringify(pedidos, null, 2)); } main().catch(console.error).finally(() => prisma.$disconnect());
