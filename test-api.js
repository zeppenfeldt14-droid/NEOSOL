async function run() {
  const headers = new Headers();
  // We can't easily fetch without auth cookie.
  // Let me just add a console.log in the route or simulate what the route does exactly.
}
run();
