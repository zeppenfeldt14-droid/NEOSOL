const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const pedidos = await prisma.pedido.findMany({ where: { empresa: { zona: 'CABA' } }, include: { empresa: true, detalles: true } }); console.log(JSON.stringify(pedidos.filter(p => p.detalles.length === 1 && p.detalles[0].productoNombre.includes('Surtido')), null, 2)); } main().catch(console.error).finally(() => prisma.$disconnect());
