import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const cutoff = new Date(Date.now() - 3 * 60 * 60 * 1000); const companies = await prisma.empresa.findMany({ where: { actualizadoEn: { gte: cutoff }, zona: 'CABA' }, include: { acciones: true, alertas: true, notasPlanificador: true, pedidos: true, ticketsSoporte: true, visitas: true } }); let toDelete = 0; let skipped = 0; for (const c of companies) { const hasRelations = c.acciones.length > 0 || c.alertas.length > 0 || c.notasPlanificador.length > 0 || c.pedidos.length > 0 || c.ticketsSoporte.length > 0 || c.visitas.length > 0 || (c.notas && c.notas.trim() !== '') || (c.vendedorAsignado && c.vendedorAsignado.trim() !== ''); if (hasRelations) { skipped++; } else { toDelete++; } } console.log('To Delete:', toDelete, 'Skipped:', skipped, 'Total:', companies.length); } main().finally(() => prisma.$disconnect());
