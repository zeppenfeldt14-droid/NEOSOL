import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const empresas = await prisma.empresa.findMany({ where: { zona: 'CABA', vendedorAsignado: 'Elarez' }, select: { nombre: true, estado: true } }); console.log(empresas.filter(e => e.estado === 'Activo' || e.estado === 'prospecto')); } main().finally(() => prisma.$disconnect());
