import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { console.log(await prisma.empresa.findFirst({ where: { nombre: { contains: 'NORIA', mode: 'insensitive' } } })); } main().finally(() => prisma.$disconnect());
