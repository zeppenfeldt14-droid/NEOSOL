const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const emps = await prisma.empresa.findMany({ where: { zona: 'Zona SUR' } }); for (let emp of emps) { if (emp.direccion) { let clean = emp.direccion.replace(/HS.*/i, '').replace(/-.*/g, '').replace(/7 A.*/i, '').trim(); if (!clean) clean = emp.direccion; await prisma.empresa.update({ where: { id: emp.id }, data: { direccion: clean } }); console.log('Updated ' + emp.nombre + ' to ' + clean); } } } main().catch(console.error).finally(() => prisma.$disconnect());
