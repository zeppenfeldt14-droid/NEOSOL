--- Line 24 ---
  ShieldCheck,
  CheckCircle,
  Edit2,
  Image as ImageIcon,
  Upload,
  Globe,
  Camera,
  Download,
  Eye,
  RefreshCw,
  Users,
  Database,
  ChevronLeft,
  ChevronRight,
  Calculator,
  AlertCircle,
  Linkedin,
  Coins,
  CreditCard,
  Copy,

=====================================
--- Line 59 ---
import RequoteModal from '../components/admin/RequoteModal';
import ManagementLogsModal from '../components/admin/ManagementLogsModal';


import ReservationsList from '../components/admin/ReservationsList';
import OperationsList from '../components/admin/OperationsList';
import TransfersDashboard from '../components/admin/TransfersDashboard';
import FlightsDashboard from '../components/admin/FlightsDashboard';
import CustomersList from '../components/admin/CustomersList';
import MarketingPanel from '../components/admin/MarketingPanel';
import UsersList from '../components/admin/UsersList';
import NotificationsModule from '../components/admin/NotificationsModule';
import CommandCenter from '../components/admin/CommandCenter';
import CreditsDashboard from '../components/admin/credits/CreditsDashboard';
import CreditRulesManager from '../components/admin/CreditRulesManager';
import CotizadorAdmin from '../components/admin/CotizadorAdmin';
import { UserPulse } from '../components/admin/UserPulse';

import NominaDashboard from '../components/admin/administration/NominaDashboard';
import FacturaDashboard from '../components/admin/administration/FacturaDashboard';

=====================================
--- Line 65 ---
import TransfersDashboard from '../components/admin/TransfersDashboard';
import FlightsDashboard from '../components/admin/FlightsDashboard';
import CustomersList from '../components/admin/CustomersList';
import MarketingPanel from '../components/admin/MarketingPanel';
import UsersList from '../components/admin/UsersList';
import NotificationsModule from '../components/admin/NotificationsModule';
import CommandCenter from '../components/admin/CommandCenter';
import CreditsDashboard from '../components/admin/credits/CreditsDashboard';
import CreditRulesManager from '../components/admin/CreditRulesManager';
import CotizadorAdmin from '../components/admin/CotizadorAdmin';
import { UserPulse } from '../components/admin/UserPulse';

import NominaDashboard from '../components/admin/administration/NominaDashboard';
import FacturaDashboard from '../components/admin/administration/FacturaDashboard';
import PagosProvDashboard from '../components/admin/administration/PagosProvDashboard';
import GastosDashboard from '../components/admin/administration/GastosDashboard';
import ResumenMensualDashboard from '../components/admin/administration/ResumenMensualDashboard';
import GananciaDashboard from '../components/admin/administration/GananciaDashboard';
import { safeJsonParse } from '../utils/safeParser';
import { TransportManagement } from '../components/admin/TransportManagement';

=====================================
--- Line 113 ---
CUM CUMANA
BRM BARQUISIMETO
CNI BARINAS
LRV LOS ROQUES
POS TRINIDAD
PTY PANAMA
SDQ STO DOMINGO RD
PUJ PUNTA CANA
MAD MADRID`;

export default function AdminDashboard({ user }: AdminProps) {
  const [location] = useLocation();
  const [activeTab, setActiveTab] = useState('inicio');
  const [useFallback, setUseFallback] = useState(false);
  const [inventorySubTab, setInventorySubTab] = useState<'hotels' | 'fullday' | 'packages' | 'aereos' | 'routes' | 'proveedores'>('hotels');
  const [operationsSubTab, setOperationsSubTab] = useState<'transfers' | 'flights'>('transfers');
  const [inventorySearch, setInventorySearch] = useState('');
  const [showSimulator, setShowSimulator] = useState(false);

  // Función auxiliar para obtener el precio final de una cotización

=====================================
--- Line 137 ---

  useEffect(() => {
    if (location === '/admin') setActiveTab('inicio');
    else if (location.includes('/inventory')) setActiveTab('inventory');
    else if (location.includes('/transfers')) setActiveTab('transfers');
    else if (location.includes('/quotes')) setActiveTab('quotes');
    else if (location.includes('/reservations')) setActiveTab('bookings');
    else if (location.includes('/credits')) setActiveTab('credits');
    else if (location.includes('/sales')) setActiveTab('operations');
    else if (location.includes('/operations_logistics')) setActiveTab('operations_logistics');
    else if (location.includes('/users')) setActiveTab('users');
    else if (location.includes('/customers')) setActiveTab('customers');
    else if (location.includes('/notifications')) setActiveTab('notifications');
    else if (location.includes('/marketing')) setActiveTab('marketing');
    else if (location.includes('/webconfig')) setActiveTab('settings');
    else if (location.includes('/nomina')) setActiveTab('nomina');
    else if (location.includes('/factura')) setActiveTab('factura');
    else if (location.includes('/pagos-prov')) setActiveTab('pagos-prov');
    else if (location.includes('/gastos')) setActiveTab('gastos');
    else if (location.includes('/resumen')) setActiveTab('resumen');

=====================================
--- Line 225 ---
  const [modalTab, setModalTab] = useState<'hotel' | 'tarifas'>('hotel');

  const [activeCoupons, setActiveCoupons] = useState<any[]>([]);

  const [config, setConfig] = useState<Record<string, string>>({});
  const [savingConfig, setSavingConfig] = useState(false);
  const [deviceMode, setDeviceMode] = useState<'pc' | 'mobile' | 'currency' | 'credit'>('pc');
  const [selectedOperation, setSelectedOperation] = useState<Operation | null>(null);
  const [opFilter, setOpFilter] = useState<'pendientes' | 'activas' | 'historial' | 'todas'>('pendientes');
  const { 
    hotels, quotes, users, setQuotes, refreshData, loadAdminData, zonas, routes, origenes,
    activeCurrency, formatPrice, config: activeConfig, providers, setProviders,
    setHotels, setReservations, setOperations
  } = useGlobalData();
  const [adminDataLoaded, setAdminDataLoaded] = useState(false);

  // Carga bajo demanda de datos administrativos al entrar a cualquier sección de gestión
  useEffect(() => {
    const adminTabs = ['quotes', 'bookings', 'operations', 'operations_logistics', 'customers', 'users', 'marketing', 'settings', 'inventory', 'transfers', 'credits', 'nomina', 'factura', 'pagos-prov', 'gastos', 'resumen', 'ganancia'];
    if (!adminDataLoaded && adminTabs.includes(activeTab)) {

=====================================
--- Line 233 ---
  const [opFilter, setOpFilter] = useState<'pendientes' | 'activas' | 'historial' | 'todas'>('pendientes');
  const { 
    hotels, quotes, users, setQuotes, refreshData, loadAdminData, zonas, routes, origenes,
    activeCurrency, formatPrice, config: activeConfig, providers, setProviders,
    setHotels, setReservations, setOperations
  } = useGlobalData();
  const [adminDataLoaded, setAdminDataLoaded] = useState(false);

  // Carga bajo demanda de datos administrativos al entrar a cualquier sección de gestión
  useEffect(() => {
    const adminTabs = ['quotes', 'bookings', 'operations', 'operations_logistics', 'customers', 'users', 'marketing', 'settings', 'inventory', 'transfers', 'credits', 'nomina', 'factura', 'pagos-prov', 'gastos', 'resumen', 'ganancia'];
    if (!adminDataLoaded && adminTabs.includes(activeTab)) {
      console.log(`[Admin] Cargando datos bajo demanda para pestaña: ${activeTab}`);
      loadAdminData?.().then(() => {
        console.log(`[Admin] Datos administrativos cargados con éxito. Cotizaciones: ${quotes?.length || 0}`);
        setAdminDataLoaded(true);
      });
    }

    // Escuchar evento de refresco desde el cotizador interno (v52)

=====================================
--- Line 248 ---
        setAdminDataLoaded(true);
      });
    }

    // Escuchar evento de refresco desde el cotizador interno (v52)
    const handleRefresh = () => loadAdminData?.();
    window.addEventListener('refresh_admin_data', handleRefresh);
    return () => window.removeEventListener('refresh_admin_data', handleRefresh);
  }, [activeTab, adminDataLoaded, loadAdminData, quotes?.length]);

  const { isMaster, isDataMaster, userAlias } = useMemo(() => {
    const levelStr = localStorage.getItem('user_level');
    const l = levelStr ? parseInt(levelStr) : 3;
    const r = localStorage.getItem('staff_user_role');
    const a = localStorage.getItem('staff_user_alias');
    
    // Log de diagnóstico para asegurar visibilidad de datos
    console.log('[Auth Debug] Admin Permissions:', { level: l, role: r, alias: a });
    
    const name = localStorage.getItem('staff_user');

=====================================
--- Line 249 ---
      });
    }

    // Escuchar evento de refresco desde el cotizador interno (v52)
    const handleRefresh = () => loadAdminData?.();
    window.addEventListener('refresh_admin_data', handleRefresh);
    return () => window.removeEventListener('refresh_admin_data', handleRefresh);
  }, [activeTab, adminDataLoaded, loadAdminData, quotes?.length]);

  const { isMaster, isDataMaster, userAlias } = useMemo(() => {
    const levelStr = localStorage.getItem('user_level');
    const l = levelStr ? parseInt(levelStr) : 3;
    const r = localStorage.getItem('staff_user_role');
    const a = localStorage.getItem('staff_user_alias');
    
    // Log de diagnóstico para asegurar visibilidad de datos
    console.log('[Auth Debug] Admin Permissions:', { level: l, role: r, alias: a });
    
    const name = localStorage.getItem('staff_user');
    const isSuperUser = r === 'Gerente General' || r === 'Gerente Operaciones' || a === 'Gerente General' || a === 'Gerente Operaciones' || name === 'Admin' || name === 'Administrador';

=====================================
--- Line 251 ---

    // Escuchar evento de refresco desde el cotizador interno (v52)
    const handleRefresh = () => loadAdminData?.();
    window.addEventListener('refresh_admin_data', handleRefresh);
    return () => window.removeEventListener('refresh_admin_data', handleRefresh);
  }, [activeTab, adminDataLoaded, loadAdminData, quotes?.length]);

  const { isMaster, isDataMaster, userAlias } = useMemo(() => {
    const levelStr = localStorage.getItem('user_level');
    const l = levelStr ? parseInt(levelStr) : 3;
    const r = localStorage.getItem('staff_user_role');
    const a = localStorage.getItem('staff_user_alias');
    
    // Log de diagnóstico para asegurar visibilidad de datos
    console.log('[Auth Debug] Admin Permissions:', { level: l, role: r, alias: a });
    
    const name = localStorage.getItem('staff_user');
    const isSuperUser = r === 'Gerente General' || r === 'Gerente Operaciones' || a === 'Gerente General' || a === 'Gerente Operaciones' || name === 'Admin' || name === 'Administrador';
    const m = isSuperUser;
    const dm = l === 1 || l === 2 || m; // Nivel 1, 2 y Roles de Gerencia tienen acceso global a datos

=====================================
--- Line 252 ---
    // Escuchar evento de refresco desde el cotizador interno (v52)
    const handleRefresh = () => loadAdminData?.();
    window.addEventListener('refresh_admin_data', handleRefresh);
    return () => window.removeEventListener('refresh_admin_data', handleRefresh);
  }, [activeTab, adminDataLoaded, loadAdminData, quotes?.length]);

  const { isMaster, isDataMaster, userAlias } = useMemo(() => {
    const levelStr = localStorage.getItem('user_level');
    const l = levelStr ? parseInt(levelStr) : 3;
    const r = localStorage.getItem('staff_user_role');
    const a = localStorage.getItem('staff_user_alias');
    
    // Log de diagnóstico para asegurar visibilidad de datos
    console.log('[Auth Debug] Admin Permissions:', { level: l, role: r, alias: a });
    
    const name = localStorage.getItem('staff_user');
    const isSuperUser = r === 'Gerente General' || r === 'Gerente Operaciones' || a === 'Gerente General' || a === 'Gerente Operaciones' || name === 'Admin' || name === 'Administrador';
    const m = isSuperUser;
    const dm = l === 1 || l === 2 || m; // Nivel 1, 2 y Roles de Gerencia tienen acceso global a datos


=====================================
--- Line 255 ---
    return () => window.removeEventListener('refresh_admin_data', handleRefresh);
  }, [activeTab, adminDataLoaded, loadAdminData, quotes?.length]);

  const { isMaster, isDataMaster, userAlias } = useMemo(() => {
    const levelStr = localStorage.getItem('user_level');
    const l = levelStr ? parseInt(levelStr) : 3;
    const r = localStorage.getItem('staff_user_role');
    const a = localStorage.getItem('staff_user_alias');
    
    // Log de diagnóstico para asegurar visibilidad de datos
    console.log('[Auth Debug] Admin Permissions:', { level: l, role: r, alias: a });
    
    const name = localStorage.getItem('staff_user');
    const isSuperUser = r === 'Gerente General' || r === 'Gerente Operaciones' || a === 'Gerente General' || a === 'Gerente Operaciones' || name === 'Admin' || name === 'Administrador';
    const m = isSuperUser;
    const dm = l === 1 || l === 2 || m; // Nivel 1, 2 y Roles de Gerencia tienen acceso global a datos

    return { isMaster: m, isDataMaster: dm, userAlias: a };
  }, [user]);


=====================================
--- Line 257 ---

  const { isMaster, isDataMaster, userAlias } = useMemo(() => {
    const levelStr = localStorage.getItem('user_level');
    const l = levelStr ? parseInt(levelStr) : 3;
    const r = localStorage.getItem('staff_user_role');
    const a = localStorage.getItem('staff_user_alias');
    
    // Log de diagnóstico para asegurar visibilidad de datos
    console.log('[Auth Debug] Admin Permissions:', { level: l, role: r, alias: a });
    
    const name = localStorage.getItem('staff_user');
    const isSuperUser = r === 'Gerente General' || r === 'Gerente Operaciones' || a === 'Gerente General' || a === 'Gerente Operaciones' || name === 'Admin' || name === 'Administrador';
    const m = isSuperUser;
    const dm = l === 1 || l === 2 || m; // Nivel 1, 2 y Roles de Gerencia tienen acceso global a datos

    return { isMaster: m, isDataMaster: dm, userAlias: a };
  }, [user]);

  const userModules = useMemo(() => {
    if (isMaster) {

=====================================
--- Line 258 ---
  const { isMaster, isDataMaster, userAlias } = useMemo(() => {
    const levelStr = localStorage.getItem('user_level');
    const l = levelStr ? parseInt(levelStr) : 3;
    const r = localStorage.getItem('staff_user_role');
    const a = localStorage.getItem('staff_user_alias');
    
    // Log de diagnóstico para asegurar visibilidad de datos
    console.log('[Auth Debug] Admin Permissions:', { level: l, role: r, alias: a });
    
    const name = localStorage.getItem('staff_user');
    const isSuperUser = r === 'Gerente General' || r === 'Gerente Operaciones' || a === 'Gerente General' || a === 'Gerente Operaciones' || name === 'Admin' || name === 'Administrador';
    const m = isSuperUser;
    const dm = l === 1 || l === 2 || m; // Nivel 1, 2 y Roles de Gerencia tienen acceso global a datos

    return { isMaster: m, isDataMaster: dm, userAlias: a };
  }, [user]);

  const userModules = useMemo(() => {
    if (isMaster) {
      return { 

=====================================
--- Line 259 ---
    const levelStr = localStorage.getItem('user_level');
    const l = levelStr ? parseInt(levelStr) : 3;
    const r = localStorage.getItem('staff_user_role');
    const a = localStorage.getItem('staff_user_alias');
    
    // Log de diagnóstico para asegurar visibilidad de datos
    console.log('[Auth Debug] Admin Permissions:', { level: l, role: r, alias: a });
    
    const name = localStorage.getItem('staff_user');
    const isSuperUser = r === 'Gerente General' || r === 'Gerente Operaciones' || a === 'Gerente General' || a === 'Gerente Operaciones' || name === 'Admin' || name === 'Administrador';
    const m = isSuperUser;
    const dm = l === 1 || l === 2 || m; // Nivel 1, 2 y Roles de Gerencia tienen acceso global a datos

    return { isMaster: m, isDataMaster: dm, userAlias: a };
  }, [user]);

  const userModules = useMemo(() => {
    if (isMaster) {
      return { 
        inventory: true, quotes: true, bookings: true, operations: true, users: true, 

=====================================
--- Line 260 ---
    const l = levelStr ? parseInt(levelStr) : 3;
    const r = localStorage.getItem('staff_user_role');
    const a = localStorage.getItem('staff_user_alias');
    
    // Log de diagnóstico para asegurar visibilidad de datos
    console.log('[Auth Debug] Admin Permissions:', { level: l, role: r, alias: a });
    
    const name = localStorage.getItem('staff_user');
    const isSuperUser = r === 'Gerente General' || r === 'Gerente Operaciones' || a === 'Gerente General' || a === 'Gerente Operaciones' || name === 'Admin' || name === 'Administrador';
    const m = isSuperUser;
    const dm = l === 1 || l === 2 || m; // Nivel 1, 2 y Roles de Gerencia tienen acceso global a datos

    return { isMaster: m, isDataMaster: dm, userAlias: a };
  }, [user]);

  const userModules = useMemo(() => {
    if (isMaster) {
      return { 
        inventory: true, quotes: true, bookings: true, operations: true, users: true, 
        customers: true, marketing: true, settings: true, credits: true, notifications: true, 

=====================================
--- Line 262 ---
    const a = localStorage.getItem('staff_user_alias');
    
    // Log de diagnóstico para asegurar visibilidad de datos
    console.log('[Auth Debug] Admin Permissions:', { level: l, role: r, alias: a });
    
    const name = localStorage.getItem('staff_user');
    const isSuperUser = r === 'Gerente General' || r === 'Gerente Operaciones' || a === 'Gerente General' || a === 'Gerente Operaciones' || name === 'Admin' || name === 'Administrador';
    const m = isSuperUser;
    const dm = l === 1 || l === 2 || m; // Nivel 1, 2 y Roles de Gerencia tienen acceso global a datos

    return { isMaster: m, isDataMaster: dm, userAlias: a };
  }, [user]);

  const userModules = useMemo(() => {
    if (isMaster) {
      return { 
        inventory: true, quotes: true, bookings: true, operations: true, users: true, 
        customers: true, marketing: true, settings: true, credits: true, notifications: true, 
        sales: true, administration: true, admin_nomina: true, admin_factura: true, 
        admin_pagos_prov: true, admin_gastos: true, admin_resumen: true, admin_ganancia: true 

=====================================
--- Line 263 ---
    
    // Log de diagnóstico para asegurar visibilidad de datos
    console.log('[Auth Debug] Admin Permissions:', { level: l, role: r, alias: a });
    
    const name = localStorage.getItem('staff_user');
    const isSuperUser = r === 'Gerente General' || r === 'Gerente Operaciones' || a === 'Gerente General' || a === 'Gerente Operaciones' || name === 'Admin' || name === 'Administrador';
    const m = isSuperUser;
    const dm = l === 1 || l === 2 || m; // Nivel 1, 2 y Roles de Gerencia tienen acceso global a datos

    return { isMaster: m, isDataMaster: dm, userAlias: a };
  }, [user]);

  const userModules = useMemo(() => {
    if (isMaster) {
      return { 
        inventory: true, quotes: true, bookings: true, operations: true, users: true, 
        customers: true, marketing: true, settings: true, credits: true, notifications: true, 
        sales: true, administration: true, admin_nomina: true, admin_factura: true, 
        admin_pagos_prov: true, admin_gastos: true, admin_resumen: true, admin_ganancia: true 
      };

=====================================
--- Line 265 ---
    console.log('[Auth Debug] Admin Permissions:', { level: l, role: r, alias: a });
    
    const name = localStorage.getItem('staff_user');
    const isSuperUser = r === 'Gerente General' || r === 'Gerente Operaciones' || a === 'Gerente General' || a === 'Gerente Operaciones' || name === 'Admin' || name === 'Administrador';
    const m = isSuperUser;
    const dm = l === 1 || l === 2 || m; // Nivel 1, 2 y Roles de Gerencia tienen acceso global a datos

    return { isMaster: m, isDataMaster: dm, userAlias: a };
  }, [user]);

  const userModules = useMemo(() => {
    if (isMaster) {
      return { 
        inventory: true, quotes: true, bookings: true, operations: true, users: true, 
        customers: true, marketing: true, settings: true, credits: true, notifications: true, 
        sales: true, administration: true, admin_nomina: true, admin_factura: true, 
        admin_pagos_prov: true, admin_gastos: true, admin_resumen: true, admin_ganancia: true 
      };
    }


=====================================
--- Line 268 ---
    const isSuperUser = r === 'Gerente General' || r === 'Gerente Operaciones' || a === 'Gerente General' || a === 'Gerente Operaciones' || name === 'Admin' || name === 'Administrador';
    const m = isSuperUser;
    const dm = l === 1 || l === 2 || m; // Nivel 1, 2 y Roles de Gerencia tienen acceso global a datos

    return { isMaster: m, isDataMaster: dm, userAlias: a };
  }, [user]);

  const userModules = useMemo(() => {
    if (isMaster) {
      return { 
        inventory: true, quotes: true, bookings: true, operations: true, users: true, 
        customers: true, marketing: true, settings: true, credits: true, notifications: true, 
        sales: true, administration: true, admin_nomina: true, admin_factura: true, 
        admin_pagos_prov: true, admin_gastos: true, admin_resumen: true, admin_ganancia: true 
      };
    }

    // Buscar el usuario actual en la lista global de usuarios
    const currentUser = (users || []).find((u: any) => u && (u.name === user || u.fullName === user || u.alias === user || u.email === user));


=====================================
--- Line 276 ---
    if (isMaster) {
      return { 
        inventory: true, quotes: true, bookings: true, operations: true, users: true, 
        customers: true, marketing: true, settings: true, credits: true, notifications: true, 
        sales: true, administration: true, admin_nomina: true, admin_factura: true, 
        admin_pagos_prov: true, admin_gastos: true, admin_resumen: true, admin_ganancia: true 
      };
    }

    // Buscar el usuario actual en la lista global de usuarios
    const currentUser = (users || []).find((u: any) => u && (u.name === user || u.fullName === user || u.alias === user || u.email === user));

    let modules: any = { inventory: false, quotes: true, bookings: false, operations: false, users: false, customers: false, marketing: false, settings: false, credits: false, notifications: true, sales: false };

    if (currentUser && currentUser.modules) {
      modules = { ...currentUser.modules };
    } else {
      // Fallback al localStorage
      try {
        modules = JSON.parse(localStorage.getItem('user_modules') || '{}');

=====================================
--- Line 278 ---
        inventory: true, quotes: true, bookings: true, operations: true, users: true, 
        customers: true, marketing: true, settings: true, credits: true, notifications: true, 
        sales: true, administration: true, admin_nomina: true, admin_factura: true, 
        admin_pagos_prov: true, admin_gastos: true, admin_resumen: true, admin_ganancia: true 
      };
    }

    // Buscar el usuario actual en la lista global de usuarios
    const currentUser = (users || []).find((u: any) => u && (u.name === user || u.fullName === user || u.alias === user || u.email === user));

    let modules: any = { inventory: false, quotes: true, bookings: false, operations: false, users: false, customers: false, marketing: false, settings: false, credits: false, notifications: true, sales: false };

    if (currentUser && currentUser.modules) {
      modules = { ...currentUser.modules };
    } else {
      // Fallback al localStorage
      try {
        modules = JSON.parse(localStorage.getItem('user_modules') || '{}');
      } catch (e) { }
    }

=====================================
--- Line 280 ---
        sales: true, administration: true, admin_nomina: true, admin_factura: true, 
        admin_pagos_prov: true, admin_gastos: true, admin_resumen: true, admin_ganancia: true 
      };
    }

    // Buscar el usuario actual en la lista global de usuarios
    const currentUser = (users || []).find((u: any) => u && (u.name === user || u.fullName === user || u.alias === user || u.email === user));

    let modules: any = { inventory: false, quotes: true, bookings: false, operations: false, users: false, customers: false, marketing: false, settings: false, credits: false, notifications: true, sales: false };

    if (currentUser && currentUser.modules) {
      modules = { ...currentUser.modules };
    } else {
      // Fallback al localStorage
      try {
        modules = JSON.parse(localStorage.getItem('user_modules') || '{}');
      } catch (e) { }
    }

    // FORZAR REGLAS MAESTRAS (Master Document)

=====================================
--- Line 281 ---
        admin_pagos_prov: true, admin_gastos: true, admin_resumen: true, admin_ganancia: true 
      };
    }

    // Buscar el usuario actual en la lista global de usuarios
    const currentUser = (users || []).find((u: any) => u && (u.name === user || u.fullName === user || u.alias === user || u.email === user));

    let modules: any = { inventory: false, quotes: true, bookings: false, operations: false, users: false, customers: false, marketing: false, settings: false, credits: false, notifications: true, sales: false };

    if (currentUser && currentUser.modules) {
      modules = { ...currentUser.modules };
    } else {
      // Fallback al localStorage
      try {
        modules = JSON.parse(localStorage.getItem('user_modules') || '{}');
      } catch (e) { }
    }

    // FORZAR REGLAS MAESTRAS (Master Document)
    // Nivel 2 y 3 nunca tienen acceso a Usuarios ni Configuración

=====================================
--- Line 285 ---
    // Buscar el usuario actual en la lista global de usuarios
    const currentUser = (users || []).find((u: any) => u && (u.name === user || u.fullName === user || u.alias === user || u.email === user));

    let modules: any = { inventory: false, quotes: true, bookings: false, operations: false, users: false, customers: false, marketing: false, settings: false, credits: false, notifications: true, sales: false };

    if (currentUser && currentUser.modules) {
      modules = { ...currentUser.modules };
    } else {
      // Fallback al localStorage
      try {
        modules = JSON.parse(localStorage.getItem('user_modules') || '{}');
      } catch (e) { }
    }

    // FORZAR REGLAS MAESTRAS (Master Document)
    // Nivel 2 y 3 nunca tienen acceso a Usuarios ni Configuración
    return {
      ...modules,
      inventory: !!modules.inventory,
      quotes: !!modules.quotes,

=====================================
--- Line 290 ---
    if (currentUser && currentUser.modules) {
      modules = { ...currentUser.modules };
    } else {
      // Fallback al localStorage
      try {
        modules = JSON.parse(localStorage.getItem('user_modules') || '{}');
      } catch (e) { }
    }

    // FORZAR REGLAS MAESTRAS (Master Document)
    // Nivel 2 y 3 nunca tienen acceso a Usuarios ni Configuración
    return {
      ...modules,
      inventory: !!modules.inventory,
      quotes: !!modules.quotes,
      bookings: !!modules.bookings,
      operations: !!modules.operations,
      sales: !!modules.sales,
      customers: !!modules.customers,
      marketing: !!modules.marketing,

=====================================
--- Line 301 ---
    return {
      ...modules,
      inventory: !!modules.inventory,
      quotes: !!modules.quotes,
      bookings: !!modules.bookings,
      operations: !!modules.operations,
      sales: !!modules.sales,
      customers: !!modules.customers,
      marketing: !!modules.marketing,
      credits: !!modules.credits,
      users: false, // Forzado
      settings: false // Forzado
    };
  }, [user, users, isMaster]);

  const [quoteFilter, setQuoteFilter] = useState<'original' | 'discounted' | 'coupon' | 're-cot' | 'no-discount' | 'history' | 'cot-00'>('original');
  const [quoteSearchTerm, setQuoteSearchTerm] = useState('');
  const [selectedQuote, setSelectedQuote] = useState<Quotation | null>(null);
  const [discount, setDiscount] = useState<number>(0);
  const [isCreatingQuote, setIsCreatingQuote] = useState(false);

=====================================
--- Line 304 ---
      quotes: !!modules.quotes,
      bookings: !!modules.bookings,
      operations: !!modules.operations,
      sales: !!modules.sales,
      customers: !!modules.customers,
      marketing: !!modules.marketing,
      credits: !!modules.credits,
      users: false, // Forzado
      settings: false // Forzado
    };
  }, [user, users, isMaster]);

  const [quoteFilter, setQuoteFilter] = useState<'original' | 'discounted' | 'coupon' | 're-cot' | 'no-discount' | 'history' | 'cot-00'>('original');
  const [quoteSearchTerm, setQuoteSearchTerm] = useState('');
  const [selectedQuote, setSelectedQuote] = useState<Quotation | null>(null);
  const [discount, setDiscount] = useState<number>(0);
  const [isCreatingQuote, setIsCreatingQuote] = useState(false);

  const [couponCodeInput, setCouponCodeInput] = useState<string>('');
  const [companions, setCompanions] = useState<{ name: string, type: string }[]>([]);

=====================================
--- Line 371 ---
      setPaymentPlatform('');
      setCreditMonths('');
      setCreditFrequency('Mensual');
    }
  }, [selectedQuote]);

  const isPdfRatesFile = newHotel.ratesFile && (
    newHotel.ratesFile.toLowerCase().includes('.pdf') ||
    newHotel.ratesFile.toLowerCase().includes('drive.google.com') ||
    newHotel.ratesFile.toLowerCase().includes('docs.google.com') ||
    newHotel.ratesFile.toLowerCase().includes('googleusercontent.com')
  );

  // Función para dividir el PDF en páginas individuales
  const splitPdfIntoPages = async (file: File) => {
    try {
      const arrayBuffer = await file.arrayBuffer();
      const pdfDoc = await PDFDocument.load(arrayBuffer);
      const pageCount = pdfDoc.getPageCount();
      const pagesData = [];

=====================================
--- Line 723 ---
      setDiscount(0);
      setCustomPercent('');
      setCouponCodeInput('');
    }
  }, [selectedQuote?.id]);

  const filteredQuotes = useMemo(() => {
    return (quotes || []).filter((q: Quotation) => {
      if (!q) return false;
      if (!isDataMaster) {
        const isAssignedToMe = q?.assignedTo === userAlias;
        const isUnassigned = !q?.assignedTo || q?.assignedTo === '';
        if (!isAssignedToMe && !isUnassigned) return false;
      }
      const matchesSearch = (q?.clientName || q?.client_name || '').toLowerCase().includes(quoteSearchTerm.toLowerCase()) || q?.id?.toString().includes(quoteSearchTerm);
      if (!matchesSearch) return false;
      const qId = String(q?.id || '');
      const isTerminal = ['Reserva', 'Venta Cerrada', 'Venta Concretada', 'Confirmada', 'Liquidada'].includes(q?.status || '');
      
      const isNewFolio = qId.startsWith('C-');

=====================================
--- Line 755 ---
      if (quoteFilter === 'coupon') return qId.endsWith('-02') || (!!(q as any)?.couponCode && !qId.endsWith('-03') && !qId.endsWith('-01'));
      if (quoteFilter === 're-cot') return /-0[3-9]$/.test(qId) || /-[1-9][0-9]$/.test(qId);
      if (quoteFilter === 'cot-00') return qId.endsWith('-00');
      if (quoteFilter === 'no-discount') {
        const isProcessed = q?.status !== 'Nuevo' && q?.status !== 'Por Aprobar' && q?.status !== 'Aprobado';
        return isOriginal && isProcessed && !(q as any).couponCode;
      }
      
      return true;
    });
  }, [quotes, isDataMaster, userAlias, quoteSearchTerm, quoteFilter]);

  const paginatedQuotes = useMemo(() => {
    const start = (currentPage - 1) * pageSize;
    return filteredQuotes.slice(start, start + pageSize);
  }, [filteredQuotes, currentPage, pageSize]);


  const totalPages = Math.ceil(filteredQuotes.length / pageSize);


=====================================
--- Line 771 ---


  const totalPages = Math.ceil(filteredQuotes.length / pageSize);

  // Reset page when filter changes
  useEffect(() => {
    setCurrentPage(1);
  }, [quoteFilter, quoteSearchTerm]);


  // Effect to load hotels, users, etc.

  const recordActivity = async (action: string, details: string) => {
    try {
      const loggedUserId = localStorage.getItem('staff_user_id');
      const loggedUserName = localStorage.getItem('staff_user');
      const loggedUserAlias = localStorage.getItem('staff_user_alias');
      await api.createLog({
        user_id: loggedUserId,
        user_name: loggedUserAlias || loggedUserName || user,

=====================================
--- Line 775 ---
  // Reset page when filter changes
  useEffect(() => {
    setCurrentPage(1);
  }, [quoteFilter, quoteSearchTerm]);


  // Effect to load hotels, users, etc.

  const recordActivity = async (action: string, details: string) => {
    try {
      const loggedUserId = localStorage.getItem('staff_user_id');
      const loggedUserName = localStorage.getItem('staff_user');
      const loggedUserAlias = localStorage.getItem('staff_user_alias');
      await api.createLog({
        user_id: loggedUserId,
        user_name: loggedUserAlias || loggedUserName || user,
        action_type: action,
        details: details
      });
    } catch (e) {

=====================================
--- Line 776 ---
  useEffect(() => {
    setCurrentPage(1);
  }, [quoteFilter, quoteSearchTerm]);


  // Effect to load hotels, users, etc.

  const recordActivity = async (action: string, details: string) => {
    try {
      const loggedUserId = localStorage.getItem('staff_user_id');
      const loggedUserName = localStorage.getItem('staff_user');
      const loggedUserAlias = localStorage.getItem('staff_user_alias');
      await api.createLog({
        user_id: loggedUserId,
        user_name: loggedUserAlias || loggedUserName || user,
        action_type: action,
        details: details
      });
    } catch (e) {
      console.error("Error recording activity:", e);

=====================================
--- Line 777 ---
    setCurrentPage(1);
  }, [quoteFilter, quoteSearchTerm]);


  // Effect to load hotels, users, etc.

  const recordActivity = async (action: string, details: string) => {
    try {
      const loggedUserId = localStorage.getItem('staff_user_id');
      const loggedUserName = localStorage.getItem('staff_user');
      const loggedUserAlias = localStorage.getItem('staff_user_alias');
      await api.createLog({
        user_id: loggedUserId,
        user_name: loggedUserAlias || loggedUserName || user,
        action_type: action,
        details: details
      });
    } catch (e) {
      console.error("Error recording activity:", e);
    }

=====================================
--- Line 779 ---


  // Effect to load hotels, users, etc.

  const recordActivity = async (action: string, details: string) => {
    try {
      const loggedUserId = localStorage.getItem('staff_user_id');
      const loggedUserName = localStorage.getItem('staff_user');
      const loggedUserAlias = localStorage.getItem('staff_user_alias');
      await api.createLog({
        user_id: loggedUserId,
        user_name: loggedUserAlias || loggedUserName || user,
        action_type: action,
        details: details
      });
    } catch (e) {
      console.error("Error recording activity:", e);
    }
  };


=====================================
--- Line 780 ---

  // Effect to load hotels, users, etc.

  const recordActivity = async (action: string, details: string) => {
    try {
      const loggedUserId = localStorage.getItem('staff_user_id');
      const loggedUserName = localStorage.getItem('staff_user');
      const loggedUserAlias = localStorage.getItem('staff_user_alias');
      await api.createLog({
        user_id: loggedUserId,
        user_name: loggedUserAlias || loggedUserName || user,
        action_type: action,
        details: details
      });
    } catch (e) {
      console.error("Error recording activity:", e);
    }
  };



=====================================
--- Line 896 ---
  const handleCloseModal = () => {
    setShowModal(false);
    setEditingId(null);
    setNewHotel({ rooms: [], seasons: [], photos: [], whatsapp: '' });
    setRoomName('');
    setRoomCapacity('');
    setModalTab('hotel');
  };

  const addRoom = () => {
    // [AÉREOS] Si es aerolínea, construir la ruta con flightDetails
    if (newHotel.type === 'aereo') {
      if (!flightFormOriginCode || !flightFormDestCode) return;
      
      const routeName = flightFormIsRoundTrip 
        ? `${flightFormOriginCode.toUpperCase()} ↔ ${flightFormDestCode.toUpperCase()} (R/T)`
        : `${flightFormOriginCode.toUpperCase()} → ${flightFormDestCode.toUpperCase()}`;

      const newRoom = {
        id: editingRoomId || crypto.randomUUID(),

=====================================
--- Line 1240 ---
                    </div>
                  </div>,
                  headerActions
                );
              })()}

              <CommandCenter />
            </div>
          )}

        {activeTab === 'inventory' && userModules?.inventory && (
          <div className="animate-in fade-in duration-500 print-hidden h-[calc(100vh-120px)] flex flex-col -mt-2">
            
            {/* Inyección de Botones en la Cabecera de Título de Sección (v62) */}
            {(() => {
              const headerActions = document.getElementById('admin-header-actions');
              if (!headerActions) return null;
              return createPortal(
                <div className="flex flex-wrap items-center gap-2 animate-in fade-in">
                  {/* Buscador */}

=====================================
--- Line 1325 ---
              );
            })()}

            {showSimulator && (
              <div className="animate-in slide-in-from-top duration-300 shrink-0 mt-4">
                <CurrencySimulator />
              </div>
            )}

            {/* Renderizado de Contenido según Pestaña */}
            <div className="flex-1 overflow-y-auto custom-scrollbar pr-2 min-h-0 mt-0">
              {inventorySubTab === 'proveedores' ? (
                <div className="bg-white rounded-[2.5rem] border border-gray-100 shadow-xl p-8 mb-10">
                  <div className="flex justify-between items-center mb-6">
                    <div>
                      <h4 className="text-base font-black italic text-[#0B132B] uppercase">Lista de Proveedores Generales</h4>
                      <p className="text-[9px] font-bold text-gray-400 uppercase tracking-widest mt-1">Gestión de proveedores para Hoteles, Aerolíneas y servicios generales</p>
                    </div>
                    <button
                      onClick={() => {

=====================================
--- Line 1331 ---
              </div>
            )}

            {/* Renderizado de Contenido según Pestaña */}
            <div className="flex-1 overflow-y-auto custom-scrollbar pr-2 min-h-0 mt-0">
              {inventorySubTab === 'proveedores' ? (
                <div className="bg-white rounded-[2.5rem] border border-gray-100 shadow-xl p-8 mb-10">
                  <div className="flex justify-between items-center mb-6">
                    <div>
                      <h4 className="text-base font-black italic text-[#0B132B] uppercase">Lista de Proveedores Generales</h4>
                      <p className="text-[9px] font-bold text-gray-400 uppercase tracking-widest mt-1">Gestión de proveedores para Hoteles, Aerolíneas y servicios generales</p>
                    </div>
                    <button
                      onClick={() => {
                        setEditingProvider(null);
                        setProviderFormData({
                          nombre: '',
                          contacto: '',
                          telefono: '',
                          email: '',

=====================================
--- Line 1387 ---
                          </tr>
                        ) : (
                          providers
                            .filter((p: any) => !inventorySearch || (p.name || p.nombre || '').toLowerCase().includes(inventorySearch.toLowerCase()))
                            .map((p: any) => (
                              <tr key={p.id} className="border-b border-gray-50 hover:bg-gray-50/50 transition-colors">
                                <td className="py-3 px-4 font-black uppercase text-[#0B132B]">{p.name || p.nombre}</td>
                                <td className="py-3 px-4 font-bold text-gray-500">
                                  <span className={`px-2.5 py-1 rounded-full text-[9px] font-black uppercase tracking-wider ${
                                    p.tipo === 'Chofer' ? 'bg-purple-100 text-purple-700' :
                                    p.tipo === 'Aerolínea' ? 'bg-blue-100 text-blue-700' :
                                    p.tipo === 'Hotel' ? 'bg-emerald-100 text-emerald-700' :
                                    p.tipo === 'Servicio' ? 'bg-amber-100 text-amber-700' :
                                    p.tipo === 'Mayorista' ? 'bg-pink-100 text-pink-700' :
                                    'bg-gray-100 text-gray-700'
                                  }`}>
                                    {p.tipo || 'Hotel'}
                                  </span>
                                </td>
                                <td className="py-3 px-4 font-bold text-gray-500">

=====================================
--- Line 1484 ---
                                           (h.location || '').toLowerCase().includes(inventorySearch.toLowerCase());
                      return matchesType && matchesSearch;
                    });

                    if (filtered.length === 0) {
                      return (
                        <div className="col-span-full py-20 flex flex-col items-center justify-center text-gray-400 bg-gray-50/50 rounded-[3rem] border-2 border-dashed border-gray-100">
                          <div className="w-20 h-20 bg-blue-50 rounded-full flex items-center justify-center mb-4">
                            <Globe size={36} className="text-blue-400" />
                          </div>
                          <p className="text-xs font-black uppercase tracking-widest italic text-gray-400">No hay aerolíneas cargadas</p>
                          <p className="text-[9px] font-black text-gray-300 uppercase tracking-widest mt-1">Haz clic en "Nueva Carga" para agregar la primera aerolínea</p>
                        </div>
                      );
                    }

                    return filtered.map((h: Hotel) => {
                      const safePhotos = Array.isArray(h.photos) ? h.photos : (typeof h.photos === 'string' ? [h.photos] : []);
                      const firstPhoto = safePhotos[0];
                      const routeCount = (h.rooms || []).length;

=====================================
--- Line 1485 ---
                      return matchesType && matchesSearch;
                    });

                    if (filtered.length === 0) {
                      return (
                        <div className="col-span-full py-20 flex flex-col items-center justify-center text-gray-400 bg-gray-50/50 rounded-[3rem] border-2 border-dashed border-gray-100">
                          <div className="w-20 h-20 bg-blue-50 rounded-full flex items-center justify-center mb-4">
                            <Globe size={36} className="text-blue-400" />
                          </div>
                          <p className="text-xs font-black uppercase tracking-widest italic text-gray-400">No hay aerolíneas cargadas</p>
                          <p className="text-[9px] font-black text-gray-300 uppercase tracking-widest mt-1">Haz clic en "Nueva Carga" para agregar la primera aerolínea</p>
                        </div>
                      );
                    }

                    return filtered.map((h: Hotel) => {
                      const safePhotos = Array.isArray(h.photos) ? h.photos : (typeof h.photos === 'string' ? [h.photos] : []);
                      const firstPhoto = safePhotos[0];
                      const routeCount = (h.rooms || []).length;
                      const agencyFee = (h as any).agencyFeePercent;

=====================================
--- Line 1509 ---
                            {firstPhoto ? <img src={firstPhoto} className="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500" /> : (
                              <div className="w-full h-full flex items-center justify-center bg-gradient-to-br from-blue-50 to-indigo-100">
                                <Globe size={48} className="text-blue-300" />
                              </div>
                            )}
                            <div className="absolute top-4 right-4 flex gap-2">
                              <button onClick={() => handleEdit(h, 'aereo')} className="p-3 bg-white/90 backdrop-blur shadow-lg rounded-xl text-gray-400 hover:text-[#0B132B] transition-all transform hover:scale-110"><Edit2 size={16} /></button>
                              <button onClick={() => handleDelete(h.id!, 'aereo')} className="p-3 bg-white/90 backdrop-blur shadow-lg rounded-xl text-gray-400 hover:text-red-500 transition-all transform hover:scale-110"><Trash2 size={16} /></button>
                            </div>
                            <div className="absolute bottom-3 left-3">
                              <span className="bg-blue-600 text-white text-[8px] font-black uppercase tracking-widest px-2 py-1 rounded-lg">✈ Aerolínea</span>
                            </div>
                          </div>
                          <div className="p-6 flex-1 flex flex-col gap-2">
                            <div className="flex items-start justify-between">
                              <h4 className="text-sm font-black italic text-[#0B132B] uppercase truncate">{h.name}</h4>
                              {agencyFee !== undefined && agencyFee !== null && (
                                <span className="bg-green-50 text-green-700 text-[8px] px-2 py-0.5 rounded-full font-black uppercase border border-green-100/50 whitespace-nowrap ml-2">Fee {agencyFee}%</span>
                              )}
                            </div>

=====================================
--- Line 1587 ---
                      );
                    });
                  })()
                }
                </div>
              )}
            </div>
          </div>
        )}

        {activeTab === 'transfers' && userModules?.inventory && (
          <div className="space-y-6 animate-in fade-in duration-500 print-hidden h-[calc(100vh-120px)] flex flex-col -mt-2">
            <div className="flex-1 overflow-y-auto custom-scrollbar">
              <TransportManagement />
            </div>
          </div>
        )}

        {activeTab === 'quotes' && userModules?.quotes && (
          <div className="animate-in fade-in duration-500 print-hidden h-[calc(100vh-120px)] flex flex-col -mt-2">

=====================================
--- Line 1589 ---
                  })()
                }
                </div>
              )}
            </div>
          </div>
        )}

        {activeTab === 'transfers' && userModules?.inventory && (
          <div className="space-y-6 animate-in fade-in duration-500 print-hidden h-[calc(100vh-120px)] flex flex-col -mt-2">
            <div className="flex-1 overflow-y-auto custom-scrollbar">
              <TransportManagement />
            </div>
          </div>
        )}

        {activeTab === 'quotes' && userModules?.quotes && (
          <div className="animate-in fade-in duration-500 print-hidden h-[calc(100vh-120px)] flex flex-col -mt-2">
            {/* Inyección de Controles en la Cabecera Global (v62) */}
            {(() => {

=====================================
--- Line 1595 ---
        )}

        {activeTab === 'transfers' && userModules?.inventory && (
          <div className="space-y-6 animate-in fade-in duration-500 print-hidden h-[calc(100vh-120px)] flex flex-col -mt-2">
            <div className="flex-1 overflow-y-auto custom-scrollbar">
              <TransportManagement />
            </div>
          </div>
        )}

        {activeTab === 'quotes' && userModules?.quotes && (
          <div className="animate-in fade-in duration-500 print-hidden h-[calc(100vh-120px)] flex flex-col -mt-2">
            {/* Inyección de Controles en la Cabecera Global (v62) */}
            {(() => {
              const headerActions = document.getElementById('admin-header-actions');
              if (!headerActions) return null;
              return createPortal(
                <div className="flex items-center gap-3">
                  <div className="flex bg-gray-50 p-1 rounded-xl border border-gray-100 gap-0.5">
                    {([

=====================================
--- Line 1597 ---
        {activeTab === 'transfers' && userModules?.inventory && (
          <div className="space-y-6 animate-in fade-in duration-500 print-hidden h-[calc(100vh-120px)] flex flex-col -mt-2">
            <div className="flex-1 overflow-y-auto custom-scrollbar">
              <TransportManagement />
            </div>
          </div>
        )}

        {activeTab === 'quotes' && userModules?.quotes && (
          <div className="animate-in fade-in duration-500 print-hidden h-[calc(100vh-120px)] flex flex-col -mt-2">
            {/* Inyección de Controles en la Cabecera Global (v62) */}
            {(() => {
              const headerActions = document.getElementById('admin-header-actions');
              if (!headerActions) return null;
              return createPortal(
                <div className="flex items-center gap-3">
                  <div className="flex bg-gray-50 p-1 rounded-xl border border-gray-100 gap-0.5">
                    {([
                      { key: 'original', label: 'Originales', alert: (quotes || []).filter((q: Quotation) => {
                        if (!q) return false;

=====================================
--- Line 1656 ---
                    onClick={() => setIsCreatingQuote(true)} 
                    className="bg-[#EB5A0C] text-white px-4 py-2 rounded-lg text-[9px] font-black uppercase tracking-widest shadow-md hover:scale-105 transition-all flex items-center gap-1.5 active:scale-95"
                  >
                    <Plus size={14} /> Cotizar
                  </button>
                </div>,
                headerActions
              );
            })()}

            {/* Contenedor de Tabla con Scroll y Sticky Header (v51) */}
            {isCreatingQuote ? (
              <div className="flex-1 min-h-0 overflow-y-auto">
                <CotizadorAdmin 
                  onClose={() => setIsCreatingQuote(false)} 
                  activeConfig={config}
                  hotels={hotels}
                  routes={routes}
                  activeCurrency={activeCurrency}
                />

=====================================
--- Line 1669 ---
                <CotizadorAdmin 
                  onClose={() => setIsCreatingQuote(false)} 
                  activeConfig={config}
                  hotels={hotels}
                  routes={routes}
                  activeCurrency={activeCurrency}
                />
              </div>
            ) : (
            <div className="bg-white rounded-[2.5rem] border border-gray-100 shadow-sm overflow-hidden flex flex-col flex-1">
              <div className="overflow-x-auto overflow-y-auto custom-scrollbar flex-1">
                <table className="w-full text-left border-separate border-spacing-0">
                  <thead className="sticky top-0 z-20 bg-white">
                    <tr className="text-[10px] font-black text-gray-400 uppercase tracking-[0.2em]">
                      <th className="py-5 px-6 border-b border-gray-100 bg-white bg-opacity-100">COTIZACIÓN / CLIENTE</th>
                      <th className="py-5 px-6 border-b border-gray-100 bg-white bg-opacity-100">FECHAS</th>
                      <th className="py-5 px-6 border-b border-gray-100 bg-white bg-opacity-100 text-center">TOTAL</th>
                      <th className="py-5 px-6 border-b border-gray-100 bg-white bg-opacity-100 text-center">ESTADO</th>
                      <th className="py-5 px-6 border-b border-gray-100 bg-white bg-opacity-100">ASESOR</th>
                      <th className="py-5 px-6 border-b border-gray-100 bg-white bg-opacity-100 text-right">ACCIONES</th>

=====================================
--- Line 1795 ---
                                          creditPlan: (quote as any).creditPlan || (quote as any).credit_plan,
                                          creditStatus: (quote as any).creditStatus || (quote as any).credit_status || ((quote as any).creditPlan || (quote as any).credit_plan ? 'PENDIENTE' : undefined),
                                          creditFrequency: (quote as any).creditFrequency || (quote as any).credit_frequency,
                                          creditPaymentLink: (quote as any).creditPaymentLink || (quote as any).credit_payment_link,
                                          paymentMethod: quote.paymentMethod || (quote as any).payment_method,
                                          paymentPlatform: quote.paymentPlatform || (quote as any).payment_platform,
                                          creditMonths: quote.creditMonths || (quote as any).credit_months,
                                          flightNumber: quote.flightNumber || (quote as any).technicalSheet?.flightNumber || null,
                                          flightItinerary: quote.flightItinerary || (quote as any).technicalSheet?.flightItinerary || null,
                                          flightPrice: (quote as any).flightPrice || (quote as any).flight_price || (quote as any).technicalSheet?.flightPrice || 0,
                                          quoteProcessedBy: quote.processedBy || (quote as any).processed_by || userAlias,
                                          processedBy: userAlias,
                                        });

                                        if (res.ok) {
                                          const resp = await api.updateQuote(quote.id, { status: 'Reserva', processedBy: userAlias });
                                          if (resp.ok) {
                                            setQuotes(prev => prev.map(q => q.id === quote.id ? { ...q, status: 'Reserva' } : q));
                                            showToast(`✅ Convertida a Reserva: ${nextResId}`);
                                            refreshData();

=====================================
--- Line 1796 ---
                                          creditStatus: (quote as any).creditStatus || (quote as any).credit_status || ((quote as any).creditPlan || (quote as any).credit_plan ? 'PENDIENTE' : undefined),
                                          creditFrequency: (quote as any).creditFrequency || (quote as any).credit_frequency,
                                          creditPaymentLink: (quote as any).creditPaymentLink || (quote as any).credit_payment_link,
                                          paymentMethod: quote.paymentMethod || (quote as any).payment_method,
                                          paymentPlatform: quote.paymentPlatform || (quote as any).payment_platform,
                                          creditMonths: quote.creditMonths || (quote as any).credit_months,
                                          flightNumber: quote.flightNumber || (quote as any).technicalSheet?.flightNumber || null,
                                          flightItinerary: quote.flightItinerary || (quote as any).technicalSheet?.flightItinerary || null,
                                          flightPrice: (quote as any).flightPrice || (quote as any).flight_price || (quote as any).technicalSheet?.flightPrice || 0,
                                          quoteProcessedBy: quote.processedBy || (quote as any).processed_by || userAlias,
                                          processedBy: userAlias,
                                        });

                                        if (res.ok) {
                                          const resp = await api.updateQuote(quote.id, { status: 'Reserva', processedBy: userAlias });
                                          if (resp.ok) {
                                            setQuotes(prev => prev.map(q => q.id === quote.id ? { ...q, status: 'Reserva' } : q));
                                            showToast(`✅ Convertida a Reserva: ${nextResId}`);
                                            refreshData();
                                          }

=====================================
--- Line 1800 ---
                                          paymentPlatform: quote.paymentPlatform || (quote as any).payment_platform,
                                          creditMonths: quote.creditMonths || (quote as any).credit_months,
                                          flightNumber: quote.flightNumber || (quote as any).technicalSheet?.flightNumber || null,
                                          flightItinerary: quote.flightItinerary || (quote as any).technicalSheet?.flightItinerary || null,
                                          flightPrice: (quote as any).flightPrice || (quote as any).flight_price || (quote as any).technicalSheet?.flightPrice || 0,
                                          quoteProcessedBy: quote.processedBy || (quote as any).processed_by || userAlias,
                                          processedBy: userAlias,
                                        });

                                        if (res.ok) {
                                          const resp = await api.updateQuote(quote.id, { status: 'Reserva', processedBy: userAlias });
                                          if (resp.ok) {
                                            setQuotes(prev => prev.map(q => q.id === quote.id ? { ...q, status: 'Reserva' } : q));
                                            showToast(`✅ Convertida a Reserva: ${nextResId}`);
                                            refreshData();
                                          }
                                        } else {
                                          showToast('❌ Error al crear registro de reserva');
                                        }
                                      } catch (err) { showToast('❌ Error de sistema'); }

=====================================
--- Line 1846 ---
                              onChange={async (e) => {
                                try {
                                  await api.updateQuote(quote.id, { assignedTo: e.target.value });
                                  setQuotes(prev => prev.map(q => q.id === quote.id ? { ...q, assignedTo: e.target.value } : q));
                                  showToast(`Asignado a ${e.target.value}`);
                                } catch (err) { }
                              }}
                              className="bg-gray-50 border-0 px-4 py-1.5 rounded-full text-[8px] font-black uppercase outline-none ring-1 ring-inset ring-gray-100 focus:ring-orange-500 w-full max-w-[120px] cursor-pointer"
                            >
                              <option value="">SIN ASIGNAR</option>
                              {users.filter(u => u && u.id).map(u => <option key={u.id} value={u.alias}>{u.alias}</option>)}
                            </select>
                          </td>
                          <td className="py-4 px-6 text-right">
                            <div className="flex items-center justify-end gap-2">
                              <button
                                title="Vista Previa"
                                onClick={() => window.open(`${api.getBaseUrl()}/public/quotes/${quote.id}/pdf`, '_blank')}
                                className="p-2 bg-gray-50 text-[#0B132B] rounded-xl hover:bg-gray-100 transition-all border border-gray-100 shadow-sm"
                              >

=====================================
--- Line 1868 ---
                              <button onClick={() => setSelectedQuote(quote)} className="bg-[#0B132B] text-white px-5 py-2 rounded-xl text-[9px] font-black uppercase hover:bg-orange-600 transition-all shadow-lg hover:shadow-orange-500/20">VER</button>
                            </div>
                          </td>
                        </tr>
                      ))
                    }
                  </tbody>
                </table>
              </div>

              {/* Controles de Paginación (v1.0) */}
              {totalPages > 1 && (
                <div className="py-6 px-10 border-t border-gray-100 flex items-center justify-between bg-white shrink-0">
                  <div className="text-[10px] font-black text-gray-400 uppercase tracking-widest">
                    Página {currentPage} de {totalPages} ({filteredQuotes.length} resultados)
                  </div>
                  <div className="flex items-center gap-3">
                    <button
                      disabled={currentPage === 1}
                      onClick={() => setCurrentPage(prev => prev - 1)}

=====================================
--- Line 1917 ---
                    >
                      <ChevronRight size={16} />
                    </button>
                  </div>
                </div>
              )}
            </div>
            )}
          </div>
        )}
        {activeTab === 'bookings' && userModules?.bookings && (
          <ReservationsList hotels={hotels} isDataMaster={isDataMaster} userAlias={userAlias} users={users} config={config} refreshData={refreshData} />
        )}

        {activeTab === 'credits' && userModules?.credits && (
          <CreditsDashboard />
        )}

        {activeTab === 'operations' && userModules?.sales && (
          <OperationsList

=====================================
--- Line 1918 ---
                      <ChevronRight size={16} />
                    </button>
                  </div>
                </div>
              )}
            </div>
            )}
          </div>
        )}
        {activeTab === 'bookings' && userModules?.bookings && (
          <ReservationsList hotels={hotels} isDataMaster={isDataMaster} userAlias={userAlias} users={users} config={config} refreshData={refreshData} />
        )}

        {activeTab === 'credits' && userModules?.credits && (
          <CreditsDashboard />
        )}

        {activeTab === 'operations' && userModules?.sales && (
          <OperationsList
            selectedOperation={selectedOperation}

=====================================
--- Line 1921 ---
                </div>
              )}
            </div>
            )}
          </div>
        )}
        {activeTab === 'bookings' && userModules?.bookings && (
          <ReservationsList hotels={hotels} isDataMaster={isDataMaster} userAlias={userAlias} users={users} config={config} refreshData={refreshData} />
        )}

        {activeTab === 'credits' && userModules?.credits && (
          <CreditsDashboard />
        )}

        {activeTab === 'operations' && userModules?.sales && (
          <OperationsList
            selectedOperation={selectedOperation}
            setSelectedOperation={setSelectedOperation}
            opFilter={opFilter}
            setOpFilter={setOpFilter}

=====================================
--- Line 1925 ---
          </div>
        )}
        {activeTab === 'bookings' && userModules?.bookings && (
          <ReservationsList hotels={hotels} isDataMaster={isDataMaster} userAlias={userAlias} users={users} config={config} refreshData={refreshData} />
        )}

        {activeTab === 'credits' && userModules?.credits && (
          <CreditsDashboard />
        )}

        {activeTab === 'operations' && userModules?.sales && (
          <OperationsList
            selectedOperation={selectedOperation}
            setSelectedOperation={setSelectedOperation}
            opFilter={opFilter}
            setOpFilter={setOpFilter}
            isDataMaster={isDataMaster}
            userAlias={userAlias}
            users={users}
            config={config}

=====================================
--- Line 1932 ---
          <CreditsDashboard />
        )}

        {activeTab === 'operations' && userModules?.sales && (
          <OperationsList
            selectedOperation={selectedOperation}
            setSelectedOperation={setSelectedOperation}
            opFilter={opFilter}
            setOpFilter={setOpFilter}
            isDataMaster={isDataMaster}
            userAlias={userAlias}
            users={users}
            config={config}
          />
        )}

        {activeTab === 'operations_logistics' && userModules?.operations && (
          <div className="h-full flex flex-col min-h-0">
            {(() => {
              const headerActions = document.getElementById('admin-header-actions');

=====================================
--- Line 1933 ---
        )}

        {activeTab === 'operations' && userModules?.sales && (
          <OperationsList
            selectedOperation={selectedOperation}
            setSelectedOperation={setSelectedOperation}
            opFilter={opFilter}
            setOpFilter={setOpFilter}
            isDataMaster={isDataMaster}
            userAlias={userAlias}
            users={users}
            config={config}
          />
        )}

        {activeTab === 'operations_logistics' && userModules?.operations && (
          <div className="h-full flex flex-col min-h-0">
            {(() => {
              const headerActions = document.getElementById('admin-header-actions');
              if (!headerActions) return null;

=====================================
--- Line 1938 ---
            setSelectedOperation={setSelectedOperation}
            opFilter={opFilter}
            setOpFilter={setOpFilter}
            isDataMaster={isDataMaster}
            userAlias={userAlias}
            users={users}
            config={config}
          />
        )}

        {activeTab === 'operations_logistics' && userModules?.operations && (
          <div className="h-full flex flex-col min-h-0">
            {(() => {
              const headerActions = document.getElementById('admin-header-actions');
              if (!headerActions) return null;
              return createPortal(
                <div className="flex bg-gray-50 p-1 rounded-xl border border-gray-100 gap-1 animate-in fade-in mr-2">
                  <button
                    onClick={() => setOperationsSubTab('transfers')}
                    className={`px-4 py-1.5 rounded-lg text-[8px] font-black uppercase tracking-widest transition-all whitespace-nowrap ${operationsSubTab === 'transfers' ? 'bg-[#0B132B] text-white shadow-md' : 'text-gray-400 hover:text-[#0B132B]'}`}

=====================================
--- Line 1971 ---
            {operationsSubTab === 'transfers' ? (
              <TransfersDashboard />
            ) : (
              <FlightsDashboard />
            )}
          </div>
        )}



        {activeTab === 'customers' && userModules?.customers && (
          <CustomersList quotes={quotes} />
        )}

        {activeTab === 'users' && typeof UsersList !== 'undefined' && <UsersList />}
        {activeTab === 'notifications' && userModules?.notifications && <NotificationsModule />}

        {activeTab === 'marketing' && userModules?.marketing && (
          <MarketingPanel quotes={quotes} config={config} />
        )}

=====================================
--- Line 1975 ---
            )}
          </div>
        )}



        {activeTab === 'customers' && userModules?.customers && (
          <CustomersList quotes={quotes} />
        )}

        {activeTab === 'users' && typeof UsersList !== 'undefined' && <UsersList />}
        {activeTab === 'notifications' && userModules?.notifications && <NotificationsModule />}

        {activeTab === 'marketing' && userModules?.marketing && (
          <MarketingPanel quotes={quotes} config={config} />
        )}

        {activeTab === 'nomina' && userModules?.admin_nomina && <NominaDashboard />}
        {activeTab === 'factura' && userModules?.admin_factura && <FacturaDashboard />}
        {activeTab === 'pagos-prov' && userModules?.admin_pagos_prov && <PagosProvDashboard />}

=====================================
--- Line 1976 ---
          </div>
        )}



        {activeTab === 'customers' && userModules?.customers && (
          <CustomersList quotes={quotes} />
        )}

        {activeTab === 'users' && typeof UsersList !== 'undefined' && <UsersList />}
        {activeTab === 'notifications' && userModules?.notifications && <NotificationsModule />}

        {activeTab === 'marketing' && userModules?.marketing && (
          <MarketingPanel quotes={quotes} config={config} />
        )}

        {activeTab === 'nomina' && userModules?.admin_nomina && <NominaDashboard />}
        {activeTab === 'factura' && userModules?.admin_factura && <FacturaDashboard />}
        {activeTab === 'pagos-prov' && userModules?.admin_pagos_prov && <PagosProvDashboard />}
        {activeTab === 'gastos' && userModules?.admin_gastos && <GastosDashboard />}

=====================================
--- Line 1978 ---



        {activeTab === 'customers' && userModules?.customers && (
          <CustomersList quotes={quotes} />
        )}

        {activeTab === 'users' && typeof UsersList !== 'undefined' && <UsersList />}
        {activeTab === 'notifications' && userModules?.notifications && <NotificationsModule />}

        {activeTab === 'marketing' && userModules?.marketing && (
          <MarketingPanel quotes={quotes} config={config} />
        )}

        {activeTab === 'nomina' && userModules?.admin_nomina && <NominaDashboard />}
        {activeTab === 'factura' && userModules?.admin_factura && <FacturaDashboard />}
        {activeTab === 'pagos-prov' && userModules?.admin_pagos_prov && <PagosProvDashboard />}
        {activeTab === 'gastos' && userModules?.admin_gastos && <GastosDashboard />}
        {activeTab === 'resumen' && userModules?.admin_resumen && <ResumenMensualDashboard />}
        {activeTab === 'ganancia' && userModules?.admin_ganancia && <GananciaDashboard />}

=====================================
--- Line 1982 ---
          <CustomersList quotes={quotes} />
        )}

        {activeTab === 'users' && typeof UsersList !== 'undefined' && <UsersList />}
        {activeTab === 'notifications' && userModules?.notifications && <NotificationsModule />}

        {activeTab === 'marketing' && userModules?.marketing && (
          <MarketingPanel quotes={quotes} config={config} />
        )}

        {activeTab === 'nomina' && userModules?.admin_nomina && <NominaDashboard />}
        {activeTab === 'factura' && userModules?.admin_factura && <FacturaDashboard />}
        {activeTab === 'pagos-prov' && userModules?.admin_pagos_prov && <PagosProvDashboard />}
        {activeTab === 'gastos' && userModules?.admin_gastos && <GastosDashboard />}
        {activeTab === 'resumen' && userModules?.admin_resumen && <ResumenMensualDashboard />}
        {activeTab === 'ganancia' && userModules?.admin_ganancia && <GananciaDashboard />}

        {activeTab === 'settings' && userModules?.settings && (
          <div className="space-y-6 animate-in fade-in duration-500 -mt-2">
            {(() => {

=====================================
--- Line 1983 ---
        )}

        {activeTab === 'users' && typeof UsersList !== 'undefined' && <UsersList />}
        {activeTab === 'notifications' && userModules?.notifications && <NotificationsModule />}

        {activeTab === 'marketing' && userModules?.marketing && (
          <MarketingPanel quotes={quotes} config={config} />
        )}

        {activeTab === 'nomina' && userModules?.admin_nomina && <NominaDashboard />}
        {activeTab === 'factura' && userModules?.admin_factura && <FacturaDashboard />}
        {activeTab === 'pagos-prov' && userModules?.admin_pagos_prov && <PagosProvDashboard />}
        {activeTab === 'gastos' && userModules?.admin_gastos && <GastosDashboard />}
        {activeTab === 'resumen' && userModules?.admin_resumen && <ResumenMensualDashboard />}
        {activeTab === 'ganancia' && userModules?.admin_ganancia && <GananciaDashboard />}

        {activeTab === 'settings' && userModules?.settings && (
          <div className="space-y-6 animate-in fade-in duration-500 -mt-2">
            {(() => {
              const headerActions = document.getElementById('admin-header-actions');

=====================================
--- Line 1984 ---

        {activeTab === 'users' && typeof UsersList !== 'undefined' && <UsersList />}
        {activeTab === 'notifications' && userModules?.notifications && <NotificationsModule />}

        {activeTab === 'marketing' && userModules?.marketing && (
          <MarketingPanel quotes={quotes} config={config} />
        )}

        {activeTab === 'nomina' && userModules?.admin_nomina && <NominaDashboard />}
        {activeTab === 'factura' && userModules?.admin_factura && <FacturaDashboard />}
        {activeTab === 'pagos-prov' && userModules?.admin_pagos_prov && <PagosProvDashboard />}
        {activeTab === 'gastos' && userModules?.admin_gastos && <GastosDashboard />}
        {activeTab === 'resumen' && userModules?.admin_resumen && <ResumenMensualDashboard />}
        {activeTab === 'ganancia' && userModules?.admin_ganancia && <GananciaDashboard />}

        {activeTab === 'settings' && userModules?.settings && (
          <div className="space-y-6 animate-in fade-in duration-500 -mt-2">
            {(() => {
              const headerActions = document.getElementById('admin-header-actions');
              if (!headerActions) return null;

=====================================
--- Line 1985 ---
        {activeTab === 'users' && typeof UsersList !== 'undefined' && <UsersList />}
        {activeTab === 'notifications' && userModules?.notifications && <NotificationsModule />}

        {activeTab === 'marketing' && userModules?.marketing && (
          <MarketingPanel quotes={quotes} config={config} />
        )}

        {activeTab === 'nomina' && userModules?.admin_nomina && <NominaDashboard />}
        {activeTab === 'factura' && userModules?.admin_factura && <FacturaDashboard />}
        {activeTab === 'pagos-prov' && userModules?.admin_pagos_prov && <PagosProvDashboard />}
        {activeTab === 'gastos' && userModules?.admin_gastos && <GastosDashboard />}
        {activeTab === 'resumen' && userModules?.admin_resumen && <ResumenMensualDashboard />}
        {activeTab === 'ganancia' && userModules?.admin_ganancia && <GananciaDashboard />}

        {activeTab === 'settings' && userModules?.settings && (
          <div className="space-y-6 animate-in fade-in duration-500 -mt-2">
            {(() => {
              const headerActions = document.getElementById('admin-header-actions');
              if (!headerActions) return null;
              return createPortal(

=====================================
--- Line 1986 ---
        {activeTab === 'notifications' && userModules?.notifications && <NotificationsModule />}

        {activeTab === 'marketing' && userModules?.marketing && (
          <MarketingPanel quotes={quotes} config={config} />
        )}

        {activeTab === 'nomina' && userModules?.admin_nomina && <NominaDashboard />}
        {activeTab === 'factura' && userModules?.admin_factura && <FacturaDashboard />}
        {activeTab === 'pagos-prov' && userModules?.admin_pagos_prov && <PagosProvDashboard />}
        {activeTab === 'gastos' && userModules?.admin_gastos && <GastosDashboard />}
        {activeTab === 'resumen' && userModules?.admin_resumen && <ResumenMensualDashboard />}
        {activeTab === 'ganancia' && userModules?.admin_ganancia && <GananciaDashboard />}

        {activeTab === 'settings' && userModules?.settings && (
          <div className="space-y-6 animate-in fade-in duration-500 -mt-2">
            {(() => {
              const headerActions = document.getElementById('admin-header-actions');
              if (!headerActions) return null;
              return createPortal(
                <div className="flex bg-white p-1.5 rounded-[2rem] border border-gray-100 shadow-sm flex-wrap gap-1 animate-in fade-in">

=====================================
--- Line 1987 ---

        {activeTab === 'marketing' && userModules?.marketing && (
          <MarketingPanel quotes={quotes} config={config} />
        )}

        {activeTab === 'nomina' && userModules?.admin_nomina && <NominaDashboard />}
        {activeTab === 'factura' && userModules?.admin_factura && <FacturaDashboard />}
        {activeTab === 'pagos-prov' && userModules?.admin_pagos_prov && <PagosProvDashboard />}
        {activeTab === 'gastos' && userModules?.admin_gastos && <GastosDashboard />}
        {activeTab === 'resumen' && userModules?.admin_resumen && <ResumenMensualDashboard />}
        {activeTab === 'ganancia' && userModules?.admin_ganancia && <GananciaDashboard />}

        {activeTab === 'settings' && userModules?.settings && (
          <div className="space-y-6 animate-in fade-in duration-500 -mt-2">
            {(() => {
              const headerActions = document.getElementById('admin-header-actions');
              if (!headerActions) return null;
              return createPortal(
                <div className="flex bg-white p-1.5 rounded-[2rem] border border-gray-100 shadow-sm flex-wrap gap-1 animate-in fade-in">
                  <button

=====================================
--- Line 1989 ---
          <MarketingPanel quotes={quotes} config={config} />
        )}

        {activeTab === 'nomina' && userModules?.admin_nomina && <NominaDashboard />}
        {activeTab === 'factura' && userModules?.admin_factura && <FacturaDashboard />}
        {activeTab === 'pagos-prov' && userModules?.admin_pagos_prov && <PagosProvDashboard />}
        {activeTab === 'gastos' && userModules?.admin_gastos && <GastosDashboard />}
        {activeTab === 'resumen' && userModules?.admin_resumen && <ResumenMensualDashboard />}
        {activeTab === 'ganancia' && userModules?.admin_ganancia && <GananciaDashboard />}

        {activeTab === 'settings' && userModules?.settings && (
          <div className="space-y-6 animate-in fade-in duration-500 -mt-2">
            {(() => {
              const headerActions = document.getElementById('admin-header-actions');
              if (!headerActions) return null;
              return createPortal(
                <div className="flex bg-white p-1.5 rounded-[2rem] border border-gray-100 shadow-sm flex-wrap gap-1 animate-in fade-in">
                  <button
                    onClick={() => setDeviceMode('pc')}
                    className={`flex items-center gap-2 px-6 py-2.5 rounded-[1.5rem] text-[10px] font-black uppercase tracking-widest transition-all ${deviceMode === 'pc' ? 'bg-[#0B132B] text-white shadow-lg' : 'text-gray-400 hover:text-[#0B132B] hover:bg-gray-50'}`}

=====================================
--- Line 2364 ---
                        ...providerFormData,
                        tipo: val,
                        es_porcentaje: isPct
                      });
                    }}
                    className="w-full bg-gray-50 rounded-2xl px-5 py-3.5 text-xs font-bold border-none outline-none ring-2 ring-gray-100 focus:ring-blue-500/20 transition-all mt-1"
                  >
                    <option value="Hotel">Hotel</option>
                    <option value="Mayorista">Mayorista</option>
                    <option value="Chofer">Chofer</option>
                    <option value="Aerolínea">Aerolínea</option>
                    <option value="Servicio">Servicio</option>
                  </select>
                </div>

                <div className="flex items-center pl-2 pt-4">
                  <label className="flex items-center gap-2 cursor-pointer select-none">
                    <input
                      type="checkbox"
                      checked={providerFormData.es_porcentaje}

=====================================
--- Line 2614 ---
          </div>
        </div>,
        document.body
      )}

    { showModal && createPortal(<div className="fixed inset-0 z-[200] w-screen h-screen flex items-center justify-center bg-[#0B132B]/80 backdrop-blur-sm p-4 overflow-hidden">
        <div className="bg-white w-full max-w-[98%] max-h-[95vh] rounded-[3rem] shadow-2xl flex flex-col overflow-hidden mx-auto animate-in fade-in zoom-in duration-300">

          <div className="p-5 md:p-6 border-b border-gray-100 flex items-center justify-between bg-gray-50/50 shrink-0">
            <h3 className="text-lg md:text-xl font-black italic text-[#0B132B] uppercase tracking-tighter">
              {editingId ? 'Editar' : 'Nueva Carga de'} {inventorySubTab === 'hotels' ? 'Hospedaje' : inventorySubTab === 'fullday' ? 'Full Day' : inventorySubTab === 'aereos' ? 'Aerolínea' : 'Paquete'}
            </h3>
            <div className="flex items-center gap-4">
              <div className="hidden md:flex bg-gray-100 p-1 rounded-2xl mr-2">
                <button 
                  onClick={() => setModalTab('hotel')}
                  className={`px-6 py-2 rounded-xl text-[9px] font-black uppercase tracking-widest transition-all ${modalTab === 'hotel' ? 'bg-white text-[#0B132B] shadow-sm' : 'text-gray-400 hover:text-gray-600'}`}
                >
                  {inventorySubTab === 'aereos' ? '✈️ Aerolínea' : inventorySubTab === 'fullday' ? '🏝️ Servicio' : inventorySubTab === 'packages' ? '🎁 Promo' : '🏨 Hotel'}
                </button>

=====================================
--- Line 2622 ---
          <div className="p-5 md:p-6 border-b border-gray-100 flex items-center justify-between bg-gray-50/50 shrink-0">
            <h3 className="text-lg md:text-xl font-black italic text-[#0B132B] uppercase tracking-tighter">
              {editingId ? 'Editar' : 'Nueva Carga de'} {inventorySubTab === 'hotels' ? 'Hospedaje' : inventorySubTab === 'fullday' ? 'Full Day' : inventorySubTab === 'aereos' ? 'Aerolínea' : 'Paquete'}
            </h3>
            <div className="flex items-center gap-4">
              <div className="hidden md:flex bg-gray-100 p-1 rounded-2xl mr-2">
                <button 
                  onClick={() => setModalTab('hotel')}
                  className={`px-6 py-2 rounded-xl text-[9px] font-black uppercase tracking-widest transition-all ${modalTab === 'hotel' ? 'bg-white text-[#0B132B] shadow-sm' : 'text-gray-400 hover:text-gray-600'}`}
                >
                  {inventorySubTab === 'aereos' ? '✈️ Aerolínea' : inventorySubTab === 'fullday' ? '🏝️ Servicio' : inventorySubTab === 'packages' ? '🎁 Promo' : '🏨 Hotel'}
                </button>
                <button 
                  onClick={() => {
                    if (isDirty) {
                      if (!confirm('Tienes cambios sin guardar. ¿Deseas cambiar de pestaña y perder los cambios?')) return;
                    }
                    setModalTab('tarifas');
                  }}
                  className={`px-6 py-2 rounded-xl text-[9px] font-black uppercase tracking-widest transition-all ${modalTab === 'tarifas' ? 'bg-[#0B132B] text-white shadow-sm' : 'text-gray-400 hover:text-gray-600'}`}

=====================================
--- Line 2652 ---
                }} 
                className="w-10 h-10 bg-white rounded-xl flex items-center justify-center text-gray-400 hover:text-red-500 shadow-sm border border-gray-100 transition-colors"
              >
                <X size={20} />
              </button>
            </div>
          </div>



          <div className="flex-1 overflow-y-auto p-6 md:p-8 custom-scrollbar">
            <div className="h-full">
              {modalTab === 'hotel' && (
                  <div className="grid grid-cols-1 lg:grid-cols-12 gap-10 animate-fadeIn" onClick={() => setIsDirty(true)}>
                  {/* COLUMNA IZQUIERDA: INFORMACIÓN Y CONTACTO */}
                  <div className="lg:col-span-7 space-y-6">
                    <div className="bg-white p-6 rounded-3xl border border-gray-100 shadow-sm space-y-5">
                      <InputField 
                        name="name" 
                        label={newHotel.type === 'aereo' ? "Nombre de la Aerolínea" : "Nombre del Hospedaje"} 

=====================================
--- Line 2661 ---

          <div className="flex-1 overflow-y-auto p-6 md:p-8 custom-scrollbar">
            <div className="h-full">
              {modalTab === 'hotel' && (
                  <div className="grid grid-cols-1 lg:grid-cols-12 gap-10 animate-fadeIn" onClick={() => setIsDirty(true)}>
                  {/* COLUMNA IZQUIERDA: INFORMACIÓN Y CONTACTO */}
                  <div className="lg:col-span-7 space-y-6">
                    <div className="bg-white p-6 rounded-3xl border border-gray-100 shadow-sm space-y-5">
                      <InputField 
                        name="name" 
                        label={newHotel.type === 'aereo' ? "Nombre de la Aerolínea" : "Nombre del Hospedaje"} 
                        value={newHotel.name || ''} 
                        onChange={(e: React.ChangeEvent<HTMLInputElement>) => setNewHotel(prev => ({ ...prev, name: e.target.value }))} 
                      />
                      
                      <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
                         <div className="space-y-2">
                          <p className="text-[10px] font-black text-gray-400 uppercase tracking-widest ml-1">
                            {newHotel.type === 'aereo' ? "Base / Hub Principal" : "Ubicación"}
                          </p>

=====================================
--- Line 2736 ---
                        {/* Vista Previa Unificada con Iconos */}
                        {newHotel.description && (
                          <div className="p-6 bg-gray-50/50 rounded-3xl border border-dashed border-gray-200 space-y-4">
                            <p className="text-[9px] font-black uppercase text-gray-400 tracking-[0.2em] flex items-center gap-2">
                              <Zap size={14} className="text-orange-400" /> Vista Previa Inteligente (Página Pública)
                            </p>
                            <div className="grid grid-cols-1 md:grid-cols-2 gap-x-8 gap-y-3">
                              {(newHotel.description || '').split('\n').filter(l => l.trim()).map((line, i) => {
                                const low = line.toLowerCase();
                                let Icon = CheckCircle;
                                if (low.includes('niño') || low.includes('edad') || low.includes('pax')) Icon = Users;
                                if (low.includes('check') || low.includes('entrada') || low.includes('salida') || low.includes('horario')) Icon = Clock || CheckCircle;
                                if (low.includes('cancel') || low.includes('regla') || low.includes('política')) Icon = ShieldCheck;
                                if (low.includes('pago') || low.includes('reserva') || low.includes('transferencia')) Icon = CreditCard;

                                return (
                                  <div key={i} className="flex gap-3 items-start animate-in fade-in duration-300">
                                    <Icon size={14} className="text-orange-500 shrink-0 mt-0.5" />
                                    <p className="text-[10px] font-bold text-gray-500 uppercase leading-tight tracking-tight">{line.trim()}</p>
                                  </div>

=====================================
--- Line 2918 ---
                                      <Trash2 size={14} className="mx-auto" />
                                    </button>
                                  </div>
                                ))}
                              </div>
                            );
                          })()}
                        </div>
                      )}

                      {/* [AÉREOS] Fee de Agencia — Solo visible para Aerolíneas */}
                      {newHotel.type === 'aereo' && (
                        <div className="space-y-2 pt-2 bg-blue-50/50 p-4 rounded-2xl border border-blue-100">
                          <div className="flex items-center gap-2 mb-2">
                            <DollarSign size={14} className="text-blue-500" />
                            <p className="text-[10px] font-black text-blue-700 uppercase tracking-widest">Fee de Agencia Global (%)</p>
                            <span className="bg-blue-100 text-blue-600 px-2 py-0.5 rounded-full text-[7px] font-black uppercase">Aplica a todas las rutas</span>
                          </div>
                          <div className="relative">
                            <span className="absolute right-5 top-1/2 -translate-y-1/2 text-blue-400 text-sm font-black">%</span>

=====================================
--- Line 3229 ---
                                      <div className="space-y-1">
                                        <label className="text-[7px] font-black text-gray-400 uppercase">Hora Salida Escala</label>
                                        <input type="time" value={flightFormSeg2OriginTime} onChange={e => setFlightFormSeg2OriginTime(e.target.value)} className="w-full bg-white rounded-xl px-3 py-2 text-xs font-bold text-[#0B132B] outline-none ring-1 ring-gray-100 focus:ring-blue-400/40" />
                                      </div>
                                      <div className="space-y-1">
                                        <label className="text-[7px] font-black text-gray-400 uppercase">Hora Llegada Destino</label>
                                        <input type="time" value={flightFormSeg2DestTime} onChange={e => setFlightFormSeg2DestTime(e.target.value)} className="w-full bg-white rounded-xl px-3 py-2 text-xs font-bold text-[#0B132B] outline-none ring-1 ring-gray-100 focus:ring-blue-400/40" />
                                      </div>
                                    </div>
                                    <div className="space-y-1 mt-2">
                                      <label className="text-[7px] font-black text-gray-400 uppercase">Aerolínea Operadora Segmento 2</label>
                                      <select 
                                        value={flightFormSeg2AirlineId} 
                                        onChange={e => setFlightFormSeg2AirlineId(e.target.value)}
                                        className="w-full bg-white rounded-xl px-3 py-2 text-xs font-bold text-[#0B132B] outline-none ring-1 ring-gray-100 focus:ring-blue-400/40"
                                      >
                                        <option value="">Misma aerolínea ({newHotel.name || 'Esta Aerolínea'})</option>
                                        {hotels.filter(h => h.type?.toLowerCase() === 'aereo' && h.id !== newHotel.id).map(h => (
                                          <option key={h.id} value={h.id}>{h.name}</option>
                                        ))}

=====================================
--- Line 3235 ---
                                        <input type="time" value={flightFormSeg2DestTime} onChange={e => setFlightFormSeg2DestTime(e.target.value)} className="w-full bg-white rounded-xl px-3 py-2 text-xs font-bold text-[#0B132B] outline-none ring-1 ring-gray-100 focus:ring-blue-400/40" />
                                      </div>
                                    </div>
                                    <div className="space-y-1 mt-2">
                                      <label className="text-[7px] font-black text-gray-400 uppercase">Aerolínea Operadora Segmento 2</label>
                                      <select 
                                        value={flightFormSeg2AirlineId} 
                                        onChange={e => setFlightFormSeg2AirlineId(e.target.value)}
                                        className="w-full bg-white rounded-xl px-3 py-2 text-xs font-bold text-[#0B132B] outline-none ring-1 ring-gray-100 focus:ring-blue-400/40"
                                      >
                                        <option value="">Misma aerolínea ({newHotel.name || 'Esta Aerolínea'})</option>
                                        {hotels.filter(h => h.type?.toLowerCase() === 'aereo' && h.id !== newHotel.id).map(h => (
                                          <option key={h.id} value={h.id}>{h.name}</option>
                                        ))}
                                      </select>
                                    </div>
                                  </div>
                                </div>
                              )}
                            </div>

=====================================
--- Line 3338 ---
                                        <div className="space-y-1">
                                          <label className="text-[7px] font-black text-gray-400 uppercase">Hora Salida Escala</label>
                                          <input type="time" value={flightFormRetSeg2OriginTime} onChange={e => setFlightFormRetSeg2OriginTime(e.target.value)} className="w-full bg-white rounded-xl px-3 py-2 text-xs font-bold text-[#0B132B] outline-none ring-1 ring-gray-100 focus:ring-indigo-400/40" />
                                        </div>
                                        <div className="space-y-1">
                                          <label className="text-[7px] font-black text-gray-400 uppercase">Hora Llegada Destino</label>
                                          <input type="time" value={flightFormRetSeg2DestTime} onChange={e => setFlightFormRetSeg2DestTime(e.target.value)} className="w-full bg-white rounded-xl px-3 py-2 text-xs font-bold text-[#0B132B] outline-none ring-1 ring-gray-100 focus:ring-indigo-400/40" />
                                        </div>
                                      </div>
                                      <div className="space-y-1 mt-2">
                                        <label className="text-[7px] font-black text-gray-400 uppercase">Aerolínea Operadora Segmento 2 (Retorno)</label>
                                        <select 
                                          value={flightFormRetSeg2AirlineId} 
                                          onChange={e => setFlightFormRetSeg2AirlineId(e.target.value)}
                                          className="w-full bg-white rounded-xl px-3 py-2 text-xs font-bold text-[#0B132B] outline-none ring-1 ring-gray-100 focus:ring-indigo-400/40"
                                        >
                                          <option value="">Misma aerolínea ({newHotel.name || 'Esta Aerolínea'})</option>
                                          {hotels.filter(h => h.type?.toLowerCase() === 'aereo' && h.id !== newHotel.id).map(h => (
                                            <option key={h.id} value={h.id}>{h.name}</option>
                                          ))}

=====================================
--- Line 3344 ---
                                          <input type="time" value={flightFormRetSeg2DestTime} onChange={e => setFlightFormRetSeg2DestTime(e.target.value)} className="w-full bg-white rounded-xl px-3 py-2 text-xs font-bold text-[#0B132B] outline-none ring-1 ring-gray-100 focus:ring-indigo-400/40" />
                                        </div>
                                      </div>
                                      <div className="space-y-1 mt-2">
                                        <label className="text-[7px] font-black text-gray-400 uppercase">Aerolínea Operadora Segmento 2 (Retorno)</label>
                                        <select 
                                          value={flightFormRetSeg2AirlineId} 
                                          onChange={e => setFlightFormRetSeg2AirlineId(e.target.value)}
                                          className="w-full bg-white rounded-xl px-3 py-2 text-xs font-bold text-[#0B132B] outline-none ring-1 ring-gray-100 focus:ring-indigo-400/40"
                                        >
                                          <option value="">Misma aerolínea ({newHotel.name || 'Esta Aerolínea'})</option>
                                          {hotels.filter(h => h.type?.toLowerCase() === 'aereo' && h.id !== newHotel.id).map(h => (
                                            <option key={h.id} value={h.id}>{h.name}</option>
                                          ))}
                                        </select>
                                      </div>
                                    </div>
                                  </div>
                                )}
                              </div>

=====================================
--- Line 3393 ---
                          </div>
                        ) : (
                          <div className="flex gap-2">
                            <input placeholder={inventorySubTab === 'fullday' ? 'Ej: Adulto' : 'Ej: Doble'} value={roomName} onChange={e => setRoomName(e.target.value)} className="flex-1 bg-gray-50 rounded-xl px-4 py-3 text-xs font-bold outline-none ring-1 ring-gray-100 focus:ring-orange-500/20" />
                            <input placeholder="Cap." type="number" value={roomCapacity} onChange={e => setRoomCapacity(e.target.value)} className="w-16 bg-gray-50 rounded-xl px-4 py-3 text-xs font-bold outline-none ring-1 ring-gray-100" />
                            <button onClick={() => { addRoom(); setIsDirty(true); }} className="bg-orange-500 text-white p-3 rounded-xl hover:bg-orange-600 transition-colors shadow-lg shadow-orange-100"><Plus size={18} /></button>
                          </div>
                        )}

                        {/* Lista de rutas/habitaciones */}
                        <div className="grid grid-cols-1 gap-2 max-h-[250px] overflow-y-auto pr-2 custom-scrollbar">
                          {Array.isArray(newHotel.rooms) && newHotel.rooms.filter(r => r && r.id).map((r: any) => (
                            <div key={r.id} className={`flex items-center justify-between p-3 rounded-2xl gap-2 group transition-colors border border-transparent ${newHotel.type === 'aereo' ? 'bg-gradient-to-r from-blue-50/50 to-white hover:border-blue-200' : 'bg-gray-50 hover:bg-orange-50/50 hover:border-orange-100'}`}>
                              <div className="flex-1 min-w-0">
                                {/* [AÉREOS] Mini tarjeta de vuelo en la lista */}
                                {newHotel.type === 'aereo' && r.flightDetails ? (
                                  <div className="flex items-center gap-3">
                                    <div className="text-center">
                                      <p className="text-[10px] font-black text-blue-600">{r.flightDetails.originCode}</p>
                                      <p className="text-[9px] font-bold text-gray-500">{r.flightDetails.originTime}</p>

=====================================
--- Line 3565 ---
                                      onClick={() => {
                                        const allSelected = pdfPages.every(p => p.selected);
                                        setPdfPages(pdfPages.map(p => ({ ...p, selected: !allSelected })));
                                      }}
                                      className="text-[8px] font-black text-orange-500 hover:text-orange-600 uppercase tracking-widest transition-colors"
                                    >
                                      {pdfPages.every(p => p.selected) ? 'Deseleccionar todas' : 'Seleccionar todas'}
                                    </button>
                                  )}
                                </div>
                                <div className="grid grid-cols-4 gap-2 max-h-[120px] overflow-y-auto pr-2 custom-scrollbar">
                                  {pdfPages.map((page, idx) => (
                                    <div key={page.id} className="relative group/page aspect-[3/4] bg-white rounded-lg border border-gray-100 overflow-hidden shadow-sm cursor-pointer" onClick={() => {
                                      const ns = [...pdfPages];
                                      ns[idx].selected = !ns[idx].selected;
                                      setPdfPages(ns);
                                    }}>
                                      {page.isImage ? (
                                        <img src={page.url} className="w-full h-full object-cover" alt={`Página ${idx + 1}`} />
                                      ) : (

=====================================
--- Line 3575 ---
                                <div className="grid grid-cols-4 gap-2 max-h-[120px] overflow-y-auto pr-2 custom-scrollbar">
                                  {pdfPages.map((page, idx) => (
                                    <div key={page.id} className="relative group/page aspect-[3/4] bg-white rounded-lg border border-gray-100 overflow-hidden shadow-sm cursor-pointer" onClick={() => {
                                      const ns = [...pdfPages];
                                      ns[idx].selected = !ns[idx].selected;
                                      setPdfPages(ns);
                                    }}>
                                      {page.isImage ? (
                                        <img src={page.url} className="w-full h-full object-cover" alt={`Página ${idx + 1}`} />
                                      ) : (
                                        <iframe src={`${page.url}#toolbar=0&navpanes=0&scrollbar=0`} className="w-full h-full pointer-events-none scale-150 origin-top" title={`Página ${idx + 1}`} />
                                      )}
                                      <div className={`absolute inset-0 flex items-center justify-center transition-colors ${page.selected ? 'bg-green-500/20' : 'bg-black/5'}`}>
                                        <div className={`w-5 h-5 rounded-full flex items-center justify-center border-2 transition-all ${page.selected ? 'bg-green-600 border-white scale-110' : 'bg-white border-gray-200'}`}>
                                          {page.selected && <CheckCircle size={12} className="text-white" />}
                                        </div>
                                      </div>
                                      <span className="absolute bottom-1 right-1 text-[7px] font-black bg-white/80 px-1 rounded text-gray-500">{idx + 1}</span>
                                    </div>
                                  ))}

=====================================
--- Line 3855 ---
                          }}>
                            <div style={{ display: 'flex', flexDirection: 'column' }}>
                              <span style={{ fontSize: '12px', fontWeight: 900, color: '#0B132B', textTransform: 'uppercase', letterSpacing: '0.1em' }}>
                                Tarifario de Referencia — {newHotel.name}
                              </span>
                              <span style={{ fontSize: '9px', fontWeight: 700, color: '#9ca3af', textTransform: 'uppercase' }}>
                                {isPdfRatesFile ? `PDF Bloqueado en Página ${newHotel.ratesFilePage || 1}` : 'Vista Previa de Imagen'}
                              </span>
                            </div>

                            {/* Controles de Zoom (Lupas) - Solo para Imágenes */}
                            {!isPdfRatesFile && (
                              <div style={{ display: 'flex', alignItems: 'center', gap: '8px', backgroundColor: '#f3f4f6', padding: '6px 14px', borderRadius: '14px', border: '1px solid #e5e7eb' }}>
                              <button 
                                onClick={() => setPreviewZoom(prev => Math.max(50, prev - 10))}
                                style={{ background: 'none', border: 'none', cursor: 'pointer', display: 'flex', alignItems: 'center', color: '#4b5563', padding: '2px' }}
                                title="Alejar (Zoom Out)"
                              >
                                <ZoomOut size={14} />
                              </button>

=====================================
--- Line 3974 ---
                          </div>
                          <button onClick={() => { 
                            setNewHotel(prev => ({ ...prev, seasons: [...(prev.seasons || []), { id: crypto.randomUUID(), type: 'Baja', startDate: '', endDate: '', roomPrices: {}, childPrice: 0, market: 'AMBOS', status: 'ACTIVE', allowFinancing: true, createdAt: new Date().toISOString() }] }));
                            setIsDirty(true);
                          }} className="bg-[#0B132B] text-white p-2 rounded-xl hover:bg-orange-600 transition-colors shadow-lg">
                            <Plus size={16} />
                          </button>
                        </div>
                      </div>

                      <div className="grid grid-cols-1 lg:grid-cols-2 gap-6 max-h-[65vh] overflow-y-auto pr-2 custom-scrollbar">
                        {(sortedSeasons || [])
                          .filter((s: any) => s && (seasonMarketFilter === 'TODOS' || (s.market || 'AMBOS') === seasonMarketFilter || s.market === 'AMBOS'))
                          .map((season: any) => {
                            const isExpired = season.endDate && new Date(season.endDate) < new Date();
                            const isPending = season.status === 'PENDING_APPROVAL';
                            const hasCollision = checkDateCollision(season, newHotel.seasons || []);

                            return (
                              <div 

=====================================
--- Line 4288 ---
                    }
                    
                    const hotelData = { 
                      ...newHotel, 
                      type: resolvedType,
                      internalNotes: finalNotes.trim(),
                      agency_fee_percent: newHotel.agencyFeePercent, // Formato snake_case para compatibilidad DB
                      agencyFeePercent: newHotel.agencyFeePercent    // Formato camelCase para consistencia frontend
                    };
                    const res = await api.saveHotel(hotelData, editingId);
                    if (res.ok) { refreshData(); handleCloseModal(); showToast(editingId ? (resolvedType === 'aereo' ? 'Aerolínea guardada' : 'Hotel guardado') : 'Creado con éxito'); }
                  }}
                  className="px-16 py-4 bg-[#0B132B] text-white rounded-[2rem] font-black text-xs uppercase tracking-[0.2em] shadow-xl hover:bg-orange-600 transition-all active:scale-[0.98] min-w-[240px]"
                >
                  {editingId ? 'Actualizar Registro' : 'Confirmar Carga'}
                </button>
                <button 
                  onClick={handleCloseModal} 
                  className="px-16 py-4 bg-white text-[#0B132B] rounded-[2rem] font-black text-xs uppercase tracking-widest border border-gray-200 hover:bg-gray-50 transition-all min-w-[240px]"
